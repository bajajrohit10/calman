import { Placeholder } from "@/components/ui";

export const metadata = { title: "Enquiries · Calman" };

export default function Page() {
  return <Placeholder title="Enquiries" note="Every enquiry with a filter on each column, a contains-filter on discussion notes, server-side pagination and CSV/XLSX export of the current view (spec §5.6)." />;
}
