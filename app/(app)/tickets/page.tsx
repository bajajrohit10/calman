import { Placeholder } from "@/components/ui";

export const metadata = { title: "Tickets · Calman" };

export default function Page() {
  return <Placeholder title="Tickets" note="The after-sale queue: open and escalated tickets sorted by reminder date, filterable by issue category. After-sale work never enters the §6 recommended list." />;
}
