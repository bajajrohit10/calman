import { Placeholder } from "@/components/ui";

export const metadata = { title: "Reports · Calman" };

export default function Page() {
  return <Placeholder title="Reports" note="Daily counsellor report, team summary by day, week and month, and the dismissable overdue report — all exportable (spec §5.8)." />;
}
