import { Placeholder } from "@/components/ui";

export const metadata = { title: "Quick Add · Calman" };

export default function Page() {
  return <Placeholder title="Quick Add" note="One box for a mobile number with live lookup: a new number opens call logging, an existing one shows full history and asks whether to update or supersede (spec §5.1)." />;
}
