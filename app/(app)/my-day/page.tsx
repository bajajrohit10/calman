import { Placeholder } from "@/components/ui";

export const metadata = { title: "My Day · Calman" };

export default function Page() {
  return <Placeholder title="My Day" note="Your assigned calls for today, grouped by bucket in the recommended order from spec §6, each opening the call-logging panel in place." />;
}
