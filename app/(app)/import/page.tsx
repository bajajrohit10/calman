import { Placeholder } from "@/components/ui";

export const metadata = { title: "Import · Calman" };

export default function Page() {
  return <Placeholder title="Import" note="Upload CSV or XLSX, map columns, normalise numbers, de-duplicate and review each row before importing. Skipped rows stay actionable (spec §5.7)." />;
}
